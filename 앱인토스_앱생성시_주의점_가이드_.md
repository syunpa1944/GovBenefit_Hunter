# 앱인토스 미니앱 생성 시 주의점 가이드

> 정부혜택털기(govbenefit-hunter) 프로젝트에서 실제로 겪은 문제와 해결 방식을 기록합니다.

---

## 1. 빌드 경로 2가지 — 반드시 이해할 것

이 프로젝트에는 빌드 경로가 **2가지** 존재합니다. 혼용하면 반드시 문제가 발생합니다.

### 경로 A: `ait build` (CI / 프로덕션)

```
build_static.js
  → esbuild: src/main.js → dist/web/sdk-bridge.js (SDK 번들링)
  → 복사: src/app.js → dist/web/app.js (그대로 복사)
  → 복사: index.html → dist/web/index.html (스크립트 참조 변환)
ait build
  → dist/web/ + 네이티브 번들 → govbenefit-hunter.ait
```

**동작 원리:**
- `sdk-bridge.js` = `@apps-in-toss/web-framework` SDK + `window` 바인딩 코드
- `app.js` = 비즈니스 로직 (별도 파일로 로드)
- `index.html`에 `<script src="sdk-bridge.js">` + `<script src="app.js">` 2개 태그
- **`src/main.js`에서 `app.js`를 import하면 안 됨** (이중 로딩 발생)

### 경로 B: Vite 빌드 (로컬 테스트용)

```
npx vite build
  → src/main.js를 진입점으로 번들링
  → dist/index.html + dist/assets/index-xxx.js
```

**동작 원리:**
- 단일 번들 파일에 모든 코드 포함
- `src/main.js`에서 `import './app.js'` 해야 번들에 포함
- **`build_static.js`와 충돌** — `import './app.js'`를 추가하면 경로 A에서 이중 실행

### 절대 규칙

```
src/main.js에는 import './app.js'를 넣지 않는다.
ait build 경로가 app.js를 별도로 로드하기 때문이다.
```

---

## 2. 발견된 문법 에러 및 해결

### 2-1. 함수 닫기 괄호 누락 (`src/app.js`)

**증상:** `SyntaxError: Unexpected token ')'` — Vite/esbuild 빌드 실패

**원인:** 두 함수에서 `}` 누락

```javascript
// 에러 1: onSigunguChange() — render()와 합체됨
function onSigunguChange() {
    const sigunguSelect = document.getElementById('sigunguSelect');
    if (sigunguSelect) {
        selectedSigungu = sigunguSelect.value;
    }
    // ❌ 여기에 } 가 빠져있었음
function render() {  // ← onSigunguChange와 합체
```

```javascript
// 에러 2: changeMonth() — openSheet()와 합체됨
function changeMonth(step) {
    currentMonth += step;
    if (currentMonth < 0) { currentMonth = 11; currentYear--; }
    else if (currentMonth > 11) { currentMonth = 0; currentYear++; }
    render();
    // ❌ 여기에 } 가 빠져있었음
function openSheet(dateStr, items) {  // ← changeMonth와 합체
```

**해결:** 닫기 `}` 추가 + 필요한 로직(render/updateDashboard/openSheet 호출) 삽입

### 2-2. 중복 코드 블록 (`app.js` root)

**증상:** `SyntaxError: Unexpected token 'else'` — 루트 app.js 빌드 실패

**원인:** `updateDashboard()` 함수 바깥에 동일한 코드가 그대로 방치

```javascript
    });  // updateDashboard 종료
    });  // ← 이 뒤에 중복된 else if 블록이 40줄이나 남아있었음
                } else if (item.amount) {
                    // ...복사된 코드...
                }
```

**해결:** 중복된 코드 블록 완전히 삭제

---

## 3. 배포(Upload) 관련 문제

### 3-1. `build_ait_pure.js`로 만든 AIT → "잘못된 앱 정보" 거부

**증상:** 토스 파트너 콘솔에서 "잘못된 앱 정보에요. ait build를 통해 다시 빌드해주세요"

**원인:** `build_ait_pure.js`는 AIT 포맷을 역공학으로 조립하지만, 콘솔에서 요구하는某些 메타데이터/프로토콜 필드가 누락됨

**해결:** 반드시 `npx ait build` 사용. `build_ait_pure.js`는 로컬 확인용으로만 사용.

### 3-2. AccessDenied 에러 + 상태 폴링 404

**증상:**
- 업로드 3단계까지 성공 → 4단계 상태 폴링에서 HTTP 404
- 또는 XML 응답 — `<Error><Code>AccessDenied</Code>...</Error>`

**원인 (확정):** `npx ait deploy` 실행 시 `--api-key` 누락

```yaml
# ❌ 문제 있음 — env만 설정하고 --api-key 미전달
env:
  AIT_ACCESS_TOKEN: ${{ secrets.AIT_ACCESS_TOKEN }}
run: |
  npx ait deploy --app-id 32871  # ← API 키가 전달되지 않음

# ✅ 올바른 형식
run: |
  npx ait deploy --api-key "${{ secrets.AIT_ACCESS_TOKEN }}" --app-id 32871 --timeout 120
```

**해결:**
1. `npx ait deploy --api-key "${{ secrets.AIT_ACCESS_TOKEN }}"` 명시적으로 전달
2. 또는 `npx ait token add`로 프로필에 등록 후 `--profile default` 사용
3. `trigger_deploy_pure.js`는 사용하지 않음 (수동 HTTP 호출 방식은 불안정)

### 3-3. `trigger_deploy_pure.js` 사용 금지

`trigger_deploy_pure.js`는 수동 HTTP 호출 방식으로, 상태 폴링 엔드포인트가 불안정합니다.
**반드시 `npx ait deploy` 명령어를 사용하세요.**

---

## 4. 파일 구조 및 역할

```
proj/
├── src/
│   ├── main.js          ← SDK import + window 바인딩 (8줄). app.js를 import하지 않음
│   ├── app.js           ← 전체 비즈니스 로직 + 광고 로직 (1600줄+)
│   └── App.css          ← 스타일
├── app.js               ← root 앱 로직 (CI/build_static.js에서 dist/web/app.js로 복사됨)
├── index.html           ← HTML 진입점 (386줄)
├── build_static.js      ← CI 빌더: esbuild로 SDK 번들링 + 파일 복사
├── build_ait_pure.js    ← 수동 AIT 조립 (프로덕션 사용 금지)
├── trigger_deploy_pure.js ← 수동 배포 스크립트
├── vite.config.js       ← Vite 설정 (emptyOutDir: false)
├── package.json         ← name: "govbenefit-hunter"
├── app.json             ← appName: "govbenefit-hunter"
└── .github/workflows/
    └── deploy.yml       ← CI: inject_data → build_static → ait build → ait deploy
```

---

## 5. 광고 SDK 연동 구조

### SDK 로드 경로 (build_static.js 기준)

```
index.html
  ├─ <script src="sdk-bridge.js">  ← esbuild 번들 (SDK + window 바인딩)
  └─ <script src="app.js">         ← 비즈니스 로직 (TossAds 등 window 전역 참조)
```

### 광고 슬롯 ID (상용)

| 광고 유형 | 슬롯 ID |
|-----------|---------|
| 배너 광고 | `ait.v2.live.c5633be2471a4b9c` |
| 리워드 광고 | `ait.v2.live.be0a965d07e0432b` |
| TossPixel | `7874162214141259463` |

### 광고 초기화 순서

```
window.onload
  → TossPixel pageView()
  → checkPendingReward() (강제종료 재개)
  → TossAds.initialize() → onInitialized → preloadRewardedAd()
  → graniteEvent backEvent 리스너 등록
```

### 배너 광고 삽입 위치

- Bottom Sheet 카드 리스트 내 10~15개마다 랜덤 간격으로 1개씩
- `TossAds.attachBanner(spotId, container, options)` 호출
- 실패 시 fallback HTML 배너 표시

### 리워드 광고 트리거

- 달력 날짜 셀 클릭 3~5회(랜덤) 도달 시 전면 광고 표시
- `loadFullScreenAd()` → 프리로드 → `showFullScreenAd()` → 보상 지급
- 강제종료 시 `rewardedOnExit` 플래그로 재개 지원

---

## 6. CI 워크플로우 (`deploy.yml`)

```yaml
steps:
  1. Checkout
  2. Python 설치 + inject_data.py (공공데이터 수집)
     → secrets.DATA_GO_KR_KEY 사용
  3. git commit & push (데이터 갱신)
  4. Node.js 설치 + npm install
  5. node build_static.js (SDK 번들링 + 파일 복사)
  6. npx ait build (.ait 패키지 생성)
  7. npx ait deploy --api-key ... (콘솔 업로드)
     → secrets.AIT_ACCESS_TOKEN을 --api-key로 전달
```

### CI에서 흔한 실패 원인

| 단계 | 에러 | 원인 | 해결 |
|------|------|------|------|
| 5 | esbuild 실패 | npm install 미완료 또는 OOM | `--max-old-space-size=4096` 설정 확인 |
| 6 | ait build 실패 (2026-07-26 발견, 매일 자동배포 지속 실패 원인) | `@apps-in-toss/ait-format`이 `engines: {node: ">=24"}`를 요구하는데 워크플로우가 `node-version: '18'`로 고정되어 있었음 | `.github/workflows/deploy.yml`의 `actions/setup-node@v4` → `node-version: '24'`로 상향 (로컬 개발 PC에도 Node 24+ 권장) |
| 6 | ait build 실패 | build_static.js 미실행 또는 dist/web 없음 | 5단계 선행 확인 |
| 7 | AccessDenied | `--api-key` 누락 | `--api-key "${{ secrets.AIT_ACCESS_TOKEN }}"` 명시 |
| 7 | "잘못된 앱 정보" | build_ait_pure.js로 빌드함 | `npx ait build` 사용 |

---

## 7. 체크리스트 — 빌드/배포 전 확인

- [ ] `src/app.js`에 문법 에러 없는지 (`node -e "new Function(require('fs').readFileSync('src/app.js','utf8'))"`)
- [ ] `app.js` (root)에 문법 에러 없는지
- [ ] `src/main.js`에 `import './app.js'`가 **없는**지
- [ ] `npx ait build` 성공인지 (sdk-bridge.js + app.js + index.html 검증 통과)
- [ ] 배포 시 `npx ait build`로 만든 `.ait` 파일을 사용하는지 (`build_ait_pure.js` 아님)
- [ ] 배포 명령어에 `--api-key`가 포함되어 있는지
- [ ] `secrets.AIT_ACCESS_TOKEN`이 유효한지
- [ ] `secrets.DATA_GO_KR_KEY`가 유효한지 (공공데이터 수집용)

---

## 8. 명령어 참조

```bash
# 빌드 (반드시 이 명령어 사용)
npx ait build

# 배포 (수동 — 반드시 --api-key 포함)
npx ait deploy --api-key "YOUR_API_KEY" --app-id 32871 --timeout 120

# 배포 (프로필 사용 시)
npx ait token add --api-key "YOUR_API_KEY"
npx ait deploy --profile default

# 문법 검증
node -e "try{new Function(require('fs').readFileSync('src/app.js','utf8'));console.log('OK')}catch(e){console.log('ERROR:',e.message)}"

# AIT 파일 크기 확인 (정상: 6~9MB)
dir govbenefit-hunter.ait

# 로컬 테스트 (Vite — app.js가 별도 로드되지 않으므로 광고 미작동 가능)
npx vite build
npx vite preview
```

### ⚠️ 사용하지 말 것

- `trigger_deploy_pure.js` — 수동 HTTP 호출 방식, 상태 폴링 불안정
- `build_ait_pure.js` — AIT 포맷 누락으로 "잘못된 앱 정보" 거부됨

---

## 9. 런타임 에러 참고

### 9-1. `ReferenceError: tagTranslation is not defined` (app.js:1084)

**증상:** Bottom Sheet 카드 태그 렌더링 시 크래시

**원인:** `openSheet()` 함수 내에서 `tagTranslation[t]`를 사용했으나, 해당 변수가 `openSheet()` 바깥에만 정의되어 있음

```javascript
// ❌ 문제: cardsHtml 내부에서 사용하지만 tagTranslation은 함수 바깥에 정의
${tags.map(t => `<span>#${tagTranslation[t]||t}</span>`).join('')}
```

**해결:** `openSheet()` 함수 내 `cardsHtml` 이전에 `tagTranslation`을 `const`로 정의

```javascript
function openSheet(dateStr, items) {
    // ...
    const tagTranslation = {
        "benefit": "정부지원금", "payback": "지자체환급", "free": "무료입장",
        "festival": "축제/공연/행사", "eco": "생태관광", "barrier": "무장애여행",
        "pet": "반려동물동반", "camp": "야영장", "stroller": "유모차반입",
        "wheelchair": "휠체어이용", "water": "물놀이/수영장"
    };
    const cardsHtml = displayItems.map(...);
}
```

### 9-8. `item.sidoNm` → `item.areaNm` 프로퍼티명 오류

**증상:** 카드에 시/도 이름이 항상 "전국"으로 표시

**원인:** 데이터 객체는 `areaNm` 키를 사용하지만, 코드에서는 `sidoNm`을 참조

```javascript
// ❌ 문제: item.sidoNm은 항상 undefined
const regionLabel = (item.sigunguNm || '') + (item.sidoNm || '전국');

// ✅ 올바른 참조
const regionLabel = (item.areaNm || '전국') + (item.sigunguNm ? ' ' + item.sigunguNm : '');
```

### 9-9. 자격 필터 누락

**증상:** 사용자가 자격 조건(장애인, 다자녀 등)을 선택해도 필터링 미적용

**원인:** `render()`와 `updateDashboard()`에서 `item.eligible` 검사 누락

```javascript
// ✅ render() 필터에 추가
if (item.eligible && !userEligibility.includes(item.eligible)) return false;

// ✅ updateDashboard() 필터에 추가  
if (item.eligible && !userEligibility.includes(item.eligible)) return;
```

### 9-4. 함수 닫기 괄호 누락으로 인한 함수 합체

**증상:** `SyntaxError: Unexpected token` — 빌드 실패

**원인:** 한 함수의 닫기 `}`가 빠지면 다음 함수 선언과 합체됨

```javascript
// ❌ 문제: onSigunguChange()와 render()가 합체
function onSigunguChange() {
    // ...
//  } ← 누락
function render() {  // ← onSigunguChange의 일부로 해석됨

// ❌ 문제: changeMonth()와 openSheet()가 합체
function changeMonth(step) {
    // ...
//  } ← 누락
function openSheet(dateStr, items) {  // ← changeMonth의 일부로 해석됨
```

### 9-5. 루트 `app.js` 중복 코드 블록

**증상:** `SyntaxError: Unexpected token 'else'`

**원인:** `updateDashboard()` 함수 끝나는 부분 뒤에 동일한 코드가 복사-붙여넣기 상태로 방치

```javascript
    });  // updateDashboard 종료
    });  // ← 여기 뒤에 40줄짜리 중복 else if 블록이 남아있었음
```

### 9-6. `ReferenceError: cardListArray is not defined` (app.js:1130)

**증상:** Bottom Sheet 카드 리스트 렌더링 시 크래시

**원인:** `openSheet()`에서 `.map()` 결과를 `cardsHtml`로 저장했으나, 배너 광고 삽입 코드에서 `cardListArray`를 사용

```javascript
// ❌ 문제: cardsHtml로 선언하고 cardListArray로 사용
const cardsHtml = displayItems.map((item, idx) => { ... });
// ...
if (cardListArray.length > 0) { ... }  // ← cardListArray는 정의되지 않음

// ✅ 해결: 변수명 통일
const cardListArray = displayItems.map((item, idx) => { ... });
```

### 9-7. AccessDenied 에러 (배포 시)

**증상:** S3 업로드 시 XML `AccessDenied` 반환

**원인:** `npx ait deploy`에 `--api-key` 누락

```bash
# ❌ 문제
npx ait deploy --app-id 32871

# ✅ 올바른 형식
npx ait deploy --api-key "YOUR_KEY" --app-id 32871 --timeout 120
```

---

## 10. 빌드 전 자동 검증 스크립트

```bash
# 문법 검증 (문법 에러 조기 발견)
node -e "try{new Function(require('fs').readFileSync('src/app.js','utf8'));console.log('src/app.js: OK')}catch(e){console.log('ERROR:',e.message)}"
node -e "try{new Function(require('fs').readFileSync('app.js','utf8'));console.log('app.js: OK')}catch(e){console.log('ERROR:',e.message)}"

# tagTranslation 존재 여부 확인
node -e "const c=require('fs').readFileSync('src/app.js','utf8'); if(c.includes('const tagTranslation')) console.log('tagTranslation: OK'); else console.log('MISSING: tagTranslation')"

# item.sidoNm 사용 여부 확인 (있으면 안 됨)
node -e "const c=require('fs').readFileSync('src/app.js','utf8'); if(c.includes('item.sidoNm')) console.log('WARNING: item.sidoNm still used'); else console.log('item.sidoNm: OK (removed)')"
```

---

## 11. 수정 사항을 실제로 반영/테스트하는 전체 절차 (매번 이 순서대로)

코드를 고치는 것과 그게 실제 앱에 반영되는 것은 별개다. 아래 5단계를 전부 거쳐야
콘솔/실기기에서 확인 가능한 상태가 된다. 중간에 하나라도 건너뛰면 "고쳤는데 안 바뀐다"가 발생한다.

### 1단계 — 로컬 검증 (푸시 전 필수)

```bash
node -e "try{new Function(require('fs').readFileSync('src/app.js','utf8'));console.log('OK')}catch(e){console.log('ERROR:',e.message)}"
node build_static.js        # dist/web 생성 + 자체 검증(스크립트 참조, TossAds 포함 여부) 통과 확인
npx ait build                # 로컬에서도 실제 .ait 패키징까지 성공하는지 확인 (Node 24+ 필요)
```

### 2단계 — 커밋 & 푸시

```bash
git add <바뀐 파일>
git commit -m "설명"
git fetch origin
git pull --rebase origin main   # 매일 자동 데이터 갱신 커밋과 항상 충돌 없이 정리하려면 rebase 사용
git push origin main
```

- `push`가 `.github/workflows/deploy.yml`의 `on.push.branches: [main]` 트리거를 즉시 발동시킨다.
  (밤 12시 스케줄과 별개로, 푸시할 때마다 매번 새로 빌드/업로드가 돈다.)

### 3단계 — GitHub Actions 상태 확인

```bash
node fetch_github_runs.js
# Latest Run ID, Status(in_progress/completed), Conclusion(success/failure) 및
# 각 스텝(Build AIT Package, Deploy and Upload ...)별 결과가 출력됨
```

- `Build AIT Package`가 실패하면 콘솔에 새 버전 자체가 안 올라간다 → 반드시 success 확인 후 다음 단계로.
- 실패 시 원인은 대부분 이 문서 6장의 표에 있는 케이스 중 하나(Node 버전, --api-key 누락, OOM 등).

### 4단계 — 토스 파트너 콘솔에서 "테스트" 버전 확인

1. [앱인토스 콘솔](https://apps-in-toss.toss.im/) → 해당 앱 → **버전** 탭 접속
2. "버전 내역" 목록 최상단에 방금 CI가 올린 새 번들 버전이 "검토 필요" 상태로 나타남
   (`--version-name` 규칙: `YYYYMMDD-{github.run_number}`, 메모는 "정부혜택달력 자동 빌드 배포")
3. **이 시점에는 아직 실제 서비스(정식 출시)에 반영된 게 아니다.** 해당 행의 **"테스트"** 버튼을
   눌러야 그 번들 버전을 토스 앱에서 실제로 열어볼 수 있다.
4. "검토 요청"은 정식 심사/배포용이므로, 리워드 광고 동작 확인 같은 단순 검증 목적이면
   "테스트" 링크만으로 충분하다. 정식 반영이 필요할 때만 "검토 요청"까지 진행한다.

> 이 4단계는 콘솔 로그인/버튼 클릭이 필요해 AI가 대신 해줄 수 없다. 사람이 직접 확인해야 한다.

### 5단계 — 실기기 테스트 시 확인할 것

- 리워드/배너 광고 문제 diagnosing 시, 토스 앱 원격 디버깅 콘솔(devtools)로 `console.log`를 직접 봐야 함
  (`[리워드광고] preload 요청`, `preload onEvent`, `preload onError` 등 — `src/app.js`에 이미 삽입됨)
- `preload onError`가 찍히면 코드 문제가 아니라 **토스 파트너 콘솔의 광고 슬롯(광고 그룹 ID) 자체의
  승인/활성화/노출재고 상태** 문제일 가능성이 가장 높음 → 콘솔의 광고 관리 메뉴에서 슬롯 상태 확인
